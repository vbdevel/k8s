apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: require-jwt
  namespace: default
spec:
  selector:
    matchLabels:
      app: first-app
  rules:
  - when:
    - key: request.auth.claims[service]
      values: ["wallet"]

