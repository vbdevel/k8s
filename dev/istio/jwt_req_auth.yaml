apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-auth
  namespace: istio-system
spec:
  selector:
    matchLabels:
      app: first-app
  jwtRules:
  - issuer: "https://dev-176hl1mzfw8138sm.us.auth0.com/"
    jwksUri: "https://dev-176hl1mzfw8138sm.us.auth0.com/.well-known/jwks.json"
