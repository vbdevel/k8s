apiVersion: batch/v1
kind: Job
metadata:
  name: job-{{ include "test.fullname" . }}
  annotation: 
    job: {{ .Values.podAnnotations.job }}
spec:
  template:
    spec:
      containers:
      - name: test-j
        image: busybox
        command: ["pwd"]
      restartPolicy: Never
